## 概要
LPを量産しやすいフォルダ構成へ再編し、共通化・スマホ崩れ検証の仕組み・Google広告用の安全文言LPを追加しました。本番URLが変わるため要確認（下記）。

## 主な変更

### 1. フォルダ構成の再編（量産対応）
- `index.html` を**入口ハブ**化。各LPは `lp/` 配下（`main.html` / `diagnosis.html` / `listing.html` / `_template.html`）
- 到達ページを `shared/`（contact / thanks / diagnosis）、PHPを `api/`、画像を `assets/img/`、CSVを `data/` に整理
- `lp/main.html` のインラインCSSを **`assets/css/theme.css`** に抽出し全LPで共有（1ファイル編集で全LPに反映）
- 旧 `diagnosis/`（jQuery版）を削除し、メーラーを `api/` に統合
- `shared/contact.html` を `api/mail.php` へ**実POST**（完了後 `shared/thanks.html` へリダイレクト）

### 2. UI改善
- `btn-yellow` を上品なシャンパンゴールドに調色（ローズと調和）
- 固定下部CTAの**電話番号がスマホで切れる崩れを修正**（`cta-fixed-*` クラス）
- `lp/diagnosis.html` に無料相談フォーム導線を追加（電話＋フォームの2系統）

### 3. レスポンシブ崩れ自動テスト（新規）
- `assets/js/layout-check.js`: 横スクロール / 画面外はみ出し / 文字切れを検出する純粋関数
- `tools/responsive-qa.html`: 全ページ×幅 **320–480px を全数スイープ**して合否表示
- **全ページ崩れ0件**を確認

### 4. 広告用LP（新規・main同等の高品質デザイン）
- `lp/listing.html`: Google広告審査向けの**超安全文言**LP。断定・誇大・数値・操作的表現を排除し、結果非保証を明記
- デザインは `main.html` の高品質コンポーネント（白文字ヒーロー / pain-card / 濃紺の bold-card / flow timeline / FAQ）を流用

### 5. バグ修正（診断ツール）
- `shared/diagnosis.html` のスタート見出し `.title` が `translateX(-100%)` のまま**画面外に固着して表示されない**問題を修正。初期状態を画面内にし、演出が走らない環境でも必ず表示されるフェードインに変更

### 6. ドキュメント
- `CLAUDE.md` を全面更新（構成 / 起動方法 / 量産フロー / 崩れテスト / 画面構成仕様）
- `.gitignore` 追加（18MBの配布用zip等を除外）

## レビュワーへの注意
- **本番URLが変わります**: トップLP `/index.html` → `/lp/main.html`、診断ツール `/diagnosis.html` → `/shared/diagnosis.html` 等。広告/QRで旧URLを告知済みの場合はリンク切れの可能性 → 必要なら旧パスにリダイレクトを用意します。
- `api/*.php` は本番 `fukuenyasan.jp` でのみ動作（ローカル送信不可）。
- 移動は git のリネーム検出で履歴を保持。`lp/diagnosis.html` のみ独自CSSで `theme.css` 不使用。
- `lp/listing.html` の文言はポリシー配慮した構成ですが、最終的な審査可否はGoogle側判断です。

## 動作確認
- `npx http-server fukuenyasan-lp -p 8123 -c-1` → `/tools/responsive-qa.html` で全7ページ崩れ0件
- 各LP・フォーム・診断フロー（開始→12問→結果）をスマホ幅(320/360/375/414)で確認

🤖 Generated with [Claude Code](https://claude.com/claude-code)
